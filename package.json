{
  "name": "gnaitv",
  "version": "1.0.0",
  "private": true,
  "description": "GNAI TV Cloudflare Pages application",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "validate-schemas": "node scripts/validate-schemas.mjs",
    "verify": "npm test && npm run validate-schemas",
    "pages:deploy": "npx wrangler pages deploy . --project-name=gnaitv --branch=main"
  }
}